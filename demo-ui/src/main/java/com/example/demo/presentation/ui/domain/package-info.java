/**
 * domain specific ui & logic (mvp pattern)
 * view: only ui concern
 * presenter: without depending on ui component
 * model: domain view model
 */
package com.example.demo.presentation.ui.domain;